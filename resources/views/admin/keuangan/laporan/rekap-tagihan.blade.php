@extends('layouts.app')

@section('title', 'Rekap Tagihan per Kelas')
@section('page-title', 'Rekap Tagihan per Kelas')
@section('page-subtitle', 'Pantau tagihan dan pembayaran setiap kelas')

@section('content')
@php
    $paidPercent = $grandTotal['tagihan'] > 0 ? round(($grandTotal['bayar'] / $grandTotal['tagihan']) * 100, 1) : 0;
    $byLevel = $kelasList->groupBy('jenjang')->map(fn ($items) => [
        'classes' => $items->count(),
        'students' => $items->sum('total_siswa'),
        'billed' => $items->sum('total_tagihan'),
        'paid' => $items->sum('total_bayar'),
        'remaining' => $items->sum('sisa_tagihan'),
    ]);
@endphp

<div data-finance-class-recap class="min-w-0 w-full space-y-5">
    <section class="flex flex-col gap-4 rounded-2xl bg-gradient-to-r from-brand-700 to-blue-500 p-5 text-white shadow-sm sm:flex-row sm:items-center sm:justify-between">
        <div><span class="text-xs font-bold uppercase tracking-wider text-blue-100">Periode aktif</span><h2 class="mt-1 text-xl font-extrabold">Tahun Ajaran {{ $tahunAjaran->nama_tahun_ajaran ?? '-' }}</h2><p class="mt-1 text-sm text-blue-100">{{ $tahunAjaran ? $tahunAjaran->tanggal_mulai->format('d/m/Y') . ' – ' . $tahunAjaran->tanggal_selesai->format('d/m/Y') : 'Periode belum tersedia' }}</p></div>
        <a href="{{ route('admin.keuangan.laporan.cetak-rekap-tagihan') }}" target="_blank" class="inline-flex min-h-10 items-center justify-center gap-2 self-start rounded-xl bg-white px-4 text-sm font-bold text-brand-700 no-underline hover:bg-blue-50"><i class="fas fa-print"></i>Cetak rekap</a>
    </section>

    <section class="grid grid-cols-2 gap-3 xl:grid-cols-4">
        @foreach([
            ['label' => 'Total tagihan', 'value' => 'Rp ' . number_format($grandTotal['tagihan'], 0, ',', '.'), 'desc' => 'Seluruh kelas', 'icon' => 'fas fa-file-invoice', 'tone' => 'bg-blue-50 text-blue-700'],
            ['label' => 'Total terbayar', 'value' => 'Rp ' . number_format($grandTotal['bayar'], 0, ',', '.'), 'desc' => 'Dana tervalidasi', 'icon' => 'fas fa-check-circle', 'tone' => 'bg-emerald-50 text-emerald-700'],
            ['label' => 'Total sisa', 'value' => 'Rp ' . number_format($grandTotal['sisa'], 0, ',', '.'), 'desc' => 'Belum dibayar', 'icon' => 'fas fa-wallet', 'tone' => 'bg-red-50 text-red-700'],
            ['label' => 'Persentase lunas', 'value' => $paidPercent . '%', 'desc' => 'Akumulasi pembayaran', 'icon' => 'fas fa-chart-pie', 'tone' => 'bg-violet-50 text-violet-700'],
        ] as $stat)
            <article class="min-w-0 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><span class="block text-[10px] font-bold uppercase tracking-wide text-slate-500">{{ $stat['label'] }}</span><strong class="mt-2 block truncate text-lg tabular-nums text-slate-950 sm:text-xl">{{ $stat['value'] }}</strong></div><span class="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl {{ $stat['tone'] }}"><i class="{{ $stat['icon'] }}"></i></span></div><p class="mt-4 border-t border-slate-100 pt-3 text-xs text-slate-500">{{ $stat['desc'] }}</p></article>
        @endforeach
    </section>

    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
        <header class="flex flex-wrap items-center justify-between gap-3 border-b border-slate-200 p-4 sm:p-5"><div class="flex items-center gap-3"><span class="flex h-9 w-9 items-center justify-center rounded-xl bg-blue-50 text-blue-700"><i class="fas fa-list-alt"></i></span><div><h2 class="text-lg font-extrabold text-slate-950">Pembayaran per kelas</h2><p class="mt-1 text-sm text-slate-500">Total tagihan, dana masuk, sisa, dan progres setiap kelas.</p></div></div><span class="rounded-xl bg-brand-50 px-3 py-2 text-xs font-bold text-brand-700">{{ $kelasList->count() }} kelas</span></header>

        @if($kelasList->isNotEmpty())
            <div class="divide-y divide-slate-100 lg:hidden">
                @foreach($kelasList as $kelas)
                    @php $textClass = $kelas->persentase >= 75 ? 'text-emerald-700' : ($kelas->persentase >= 50 ? 'text-amber-700' : 'text-red-700'); @endphp
                    <article class="p-4"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><strong class="block text-sm text-slate-950">{{ $kelas->nama_kelas }} · {{ $kelas->jenjang }}</strong><span class="mt-1 block truncate text-xs text-slate-500">{{ $kelas->kode_kelas }} · {{ $kelas->cabang->nama_cabang ?? 'Cabang belum diatur' }} · {{ $kelas->total_siswa }} siswa</span></div><span class="shrink-0 rounded-lg bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-600">{{ $kelas->sisa_tagihan <= 0 ? 'Lunas' : 'Berjalan' }}</span></div><dl class="mt-4 grid grid-cols-3 gap-2 text-xs"><div><dt class="text-slate-500">Tagihan</dt><dd class="mt-1 truncate font-bold tabular-nums text-slate-950">Rp {{ number_format($kelas->total_tagihan, 0, ',', '.') }}</dd></div><div><dt class="text-slate-500">Terbayar</dt><dd class="mt-1 truncate font-bold tabular-nums text-emerald-700">Rp {{ number_format($kelas->total_bayar, 0, ',', '.') }}</dd></div><div><dt class="text-slate-500">Sisa</dt><dd class="mt-1 truncate font-bold tabular-nums text-red-700">Rp {{ number_format($kelas->sisa_tagihan, 0, ',', '.') }}</dd></div></dl><div class="mt-4 flex items-center gap-3"><svg viewBox="0 0 100 8" preserveAspectRatio="none" class="h-2 min-w-0 flex-1 {{ $textClass }}" aria-label="Progres {{ $kelas->persentase }} persen"><rect width="100" height="8" rx="4" class="fill-slate-100"></rect><rect width="{{ min(100, $kelas->persentase) }}" height="8" rx="4" class="fill-current"></rect></svg><strong class="text-xs {{ $textClass }}">{{ $kelas->persentase }}%</strong></div></article>
                @endforeach
            </div>

            <div class="hidden overflow-x-auto lg:block"><table class="w-full table-fixed border-collapse text-left"><colgroup><col class="w-12"><col class="w-[18%]"><col class="w-20"><col class="w-16"><col class="w-[15%]"><col class="w-[15%]"><col class="w-[15%]"><col class="w-[15%]"><col class="w-28"></colgroup><thead class="bg-slate-50 text-[10px] font-bold uppercase tracking-wide text-slate-500"><tr><th class="px-3 py-3 text-center">No</th><th class="px-3 py-3">Kelas</th><th class="px-3 py-3">Jenjang</th><th class="px-3 py-3 text-center">Siswa</th><th class="px-3 py-3">Total tagihan</th><th class="px-3 py-3">Terbayar</th><th class="px-3 py-3">Sisa</th><th class="px-3 py-3">Persentase</th><th class="px-3 py-3 text-center">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($kelasList as $index => $kelas)@php $textClass = $kelas->persentase >= 75 ? 'text-emerald-700' : ($kelas->persentase >= 50 ? 'text-amber-700' : 'text-red-700'); @endphp<tr class="hover:bg-slate-50"><td class="px-3 py-4 text-center text-xs text-slate-500">{{ $index + 1 }}</td><td class="px-3 py-4"><strong class="block truncate text-sm text-slate-950">{{ $kelas->nama_kelas }}</strong><span class="mt-1 block truncate text-xs text-slate-500">{{ $kelas->kode_kelas }} · {{ $kelas->cabang->nama_cabang ?? '-' }}</span></td><td class="px-3 py-4"><span class="rounded-lg bg-blue-50 px-2 py-1 text-[10px] font-bold text-blue-700">{{ $kelas->jenjang }}</span></td><td class="px-3 py-4 text-center text-sm font-bold text-slate-800">{{ $kelas->total_siswa }}</td><td class="px-3 py-4 whitespace-nowrap text-xs font-bold tabular-nums text-slate-900">Rp {{ number_format($kelas->total_tagihan, 0, ',', '.') }}</td><td class="px-3 py-4 whitespace-nowrap text-xs font-bold tabular-nums text-emerald-700">Rp {{ number_format($kelas->total_bayar, 0, ',', '.') }}</td><td class="px-3 py-4 whitespace-nowrap text-xs font-bold tabular-nums {{ $kelas->sisa_tagihan > 0 ? 'text-red-700' : 'text-emerald-700' }}">Rp {{ number_format($kelas->sisa_tagihan, 0, ',', '.') }}</td><td class="px-3 py-4"><div class="flex items-center gap-2"><svg viewBox="0 0 100 8" preserveAspectRatio="none" class="h-2 min-w-0 flex-1 {{ $textClass }}" aria-label="Progres {{ $kelas->persentase }} persen"><rect width="100" height="8" rx="4" class="fill-slate-100"></rect><rect width="{{ min(100, $kelas->persentase) }}" height="8" rx="4" class="fill-current"></rect></svg><strong class="w-10 text-right text-xs {{ $textClass }}">{{ $kelas->persentase }}%</strong></div></td><td class="px-3 py-4 text-center"><span class="rounded-lg px-2 py-1 text-[10px] font-bold {{ $kelas->sisa_tagihan <= 0 ? 'bg-emerald-50 text-emerald-700' : ($kelas->persentase >= 75 ? 'bg-amber-50 text-amber-700' : ($kelas->persentase >= 50 ? 'bg-cyan-50 text-cyan-700' : 'bg-red-50 text-red-700')) }}">{{ $kelas->sisa_tagihan <= 0 ? 'Lunas' : ($kelas->persentase >= 75 ? 'Hampir lunas' : ($kelas->persentase >= 50 ? 'Dalam proses' : 'Belum bayar')) }}</span></td></tr>@endforeach</tbody><tfoot><tr class="bg-slate-50"><td colspan="3" class="px-3 py-3 text-right text-xs font-extrabold text-slate-700">Grand total</td><td class="px-3 py-3 text-center text-sm font-extrabold">{{ $kelasList->sum('total_siswa') }}</td><td class="px-3 py-3 whitespace-nowrap text-xs font-extrabold tabular-nums">Rp {{ number_format($grandTotal['tagihan'], 0, ',', '.') }}</td><td class="px-3 py-3 whitespace-nowrap text-xs font-extrabold tabular-nums text-emerald-700">Rp {{ number_format($grandTotal['bayar'], 0, ',', '.') }}</td><td class="px-3 py-3 whitespace-nowrap text-xs font-extrabold tabular-nums text-red-700">Rp {{ number_format($grandTotal['sisa'], 0, ',', '.') }}</td><td colspan="2"></td></tr></tfoot></table></div>
        @else
            <div class="flex min-h-60 flex-col items-center justify-center p-8 text-center"><span class="flex h-14 w-14 items-center justify-center rounded-2xl bg-slate-100 text-xl text-slate-400"><i class="fas fa-database"></i></span><h3 class="mt-4 text-base font-extrabold text-slate-950">Data kelas belum tersedia</h3><p class="mt-1 text-sm text-slate-500">Belum ada rekap tagihan pada tahun ajaran aktif.</p></div>
        @endif
    </section>

    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><header class="border-b border-slate-200 p-4 sm:p-5"><div class="flex items-center gap-3"><span class="flex h-9 w-9 items-center justify-center rounded-xl bg-amber-50 text-amber-700"><i class="fas fa-chart-bar"></i></span><div><h2 class="text-lg font-extrabold text-slate-950">Ringkasan per jenjang</h2><p class="mt-1 text-sm text-slate-500">Perbandingan siswa, tagihan, dan pelunasan tiap jenjang.</p></div></div></header><div class="grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-3 sm:p-5">@forelse($byLevel as $level => $data)@php $levelPercent = $data['billed'] > 0 ? round(($data['paid'] / $data['billed']) * 100, 1) : 0; $levelText = $levelPercent >= 75 ? 'text-emerald-700' : ($levelPercent >= 50 ? 'text-amber-700' : 'text-red-700'); @endphp<article class="rounded-xl border border-slate-200 p-4"><div class="flex items-start justify-between gap-3"><div><h3 class="text-base font-extrabold text-slate-950">{{ $level }}</h3><p class="mt-1 text-xs text-slate-500">{{ $data['students'] }} siswa aktif</p></div><span class="rounded-lg bg-brand-50 px-2 py-1 text-[10px] font-bold text-brand-700">{{ $data['classes'] }} kelas</span></div><dl class="mt-4 space-y-2 text-xs"><div class="flex justify-between gap-3"><dt class="text-slate-500">Total tagihan</dt><dd class="font-bold tabular-nums text-slate-950">Rp {{ number_format($data['billed'], 0, ',', '.') }}</dd></div><div class="flex justify-between gap-3"><dt class="text-slate-500">Terbayar</dt><dd class="font-bold tabular-nums text-emerald-700">Rp {{ number_format($data['paid'], 0, ',', '.') }}</dd></div><div class="flex justify-between gap-3"><dt class="text-slate-500">Sisa</dt><dd class="font-bold tabular-nums text-red-700">Rp {{ number_format($data['remaining'], 0, ',', '.') }}</dd></div></dl><svg viewBox="0 0 100 8" preserveAspectRatio="none" class="mt-4 h-2 w-full {{ $levelText }}" aria-label="Progres {{ $levelPercent }} persen"><rect width="100" height="8" rx="4" class="fill-slate-100"></rect><rect width="{{ min(100, $levelPercent) }}" height="8" rx="4" class="fill-current"></rect></svg><p class="mt-2 text-center text-xs font-bold {{ $levelText }}">{{ $levelPercent }}% lunas</p></article>@empty<div class="col-span-full py-10 text-center text-sm text-slate-500">Belum ada data jenjang.</div>@endforelse</div></section>
</div>
@endsection
